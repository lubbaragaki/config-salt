#!/usr/bin/env bash

# Set the ACL for existing files/directories
sudo setfacl -R -m u:"$(whoami)":rw /srv/salt/

# Set a default ACL so new files/subdirectories you create inherit the same permissions
sudo setfacl -R -m d:u:"$(whoami)":rw /srv/salt/
